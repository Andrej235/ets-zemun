export default interface ProfileSchema {     /**      * Backend ID of the profile      */     backendId: number;     /**      * Full description of the profile      */     description: string;     /**      * Path to the image of the profile      * MUST start with ''@images/profiles/''      * MUST end with one of the following extensions: .jpg, .jpeg, .png, .gif, .bmp, .webp,      * .tiff, .svg      * The referenced image MUST exist in the '/public/images/profiles/' folder      */     imagePath: string;     /**      * Name of the profile      */     name: string; }
