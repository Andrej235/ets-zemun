{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the document"
    },
    "url": {
      "type": "string",
      "description": "URL of the document"
    }
  },
  "required": ["title", "url"],
  "additionalProperties": false
}
